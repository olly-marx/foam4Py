cmake_minimum_required(VERSION 3.12 FATAL_ERROR)

set(CMAKE_CXX_STANDARD 11)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wextra -Wpedantic -Werror -g")

project(pyBindPatchAverage)

set(CMAKE_PACKAGE_DIR ${CMAKE_CURRENT_SOURCE_DIR})

include_directories(./)

#find_package(FOAM REQUIRED)
#include_directories(${FOAM_INCLUDE_DIRS})

message("Adding python bindings")

# Many packages need python:
find_package(Python ${CMAKE_PYVERSION} REQUIRED COMPONENTS  Interpreter Development)
include_directories(${Python_INCLUDE_DIRS})

message("Adding pybind11")
add_subdirectory(pybind11)
include_directories(${PYBIND11_INCLUDE_DIR})

set(pybind11_DIR pybind11)

add_subdirectory(patchAverage)

# Optionally, install header files
#install(FILES your_header_files DESTINATION include)


