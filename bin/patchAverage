#!/usr/bin/env python
"""
patchAverage.py

A script that demonstrates the usage of the patchAverage module in the pyBindFOAMPackage.

This script calculates the average value of a specified field over a given patch using the
`patchAverage` module.

Usage:
    ./patchAverage.py

Author:
    Oliver Marx <oliver.j.marx@gmail.com>
"""

import sys
import os

from pyBindFOAMPackage import patchAverage

def main():
    field = "p"
    patch = "movingWall"
    patchAverageInstance = patchAverage(field, patch)
    patchAverageInstance.calculateAverage()

if __name__ == "__main__":
    main()

